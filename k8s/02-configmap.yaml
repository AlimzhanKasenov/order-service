apiVersion: v1
kind: ConfigMap
metadata:
  name: order-service-config
data:
  SERVER_PORT: "8000"

  SERVICE_NAME: order-service
  LOG_LEVEL: INFO

  DB_HOST: order-postgresql
  DB_PORT: "5432"
  DB_NAME: users
  DB_SSLMODE: disable
