apiVersion: apps/v1
kind: Deployment
metadata:
  name: billing-service
  namespace: distributed-transactions
spec:
  replicas: 1

  selector:
    matchLabels:
      app: billing-service

  template:
    metadata:
      labels:
        app: billing-service

    spec:
      containers:
        - name: billing-service
          image: alimzhankassenov/billing-service:distributed-transactions
          imagePullPolicy: Always

          ports:
            - containerPort: 8001

          env:
            - name: SERVER_PORT
              value: "8001"

            - name: DB_HOST
              value: billing-postgres

            - name: DB_PORT
              value: "5432"

            - name: DB_NAME
              value: billing

            - name: DB_SSLMODE
              value: disable

            - name: DB_USERNAME
              valueFrom:
                secretKeyRef:
                  name: distributed-transactions-secrets
                  key: BILLING_DB_USERNAME

            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: distributed-transactions-secrets
                  key: BILLING_DB_PASSWORD

            - name: KAFKA_BROKER
              value: kafka:29092

          readinessProbe:
            httpGet:
              path: /health
              port: 8001
            initialDelaySeconds: 5
            periodSeconds: 5
            failureThreshold: 12

          livenessProbe:
            httpGet:
              path: /health
              port: 8001
            initialDelaySeconds: 20
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: billing-service
  namespace: distributed-transactions
spec:
  selector:
    app: billing-service

  ports:
    - port: 8001
      targetPort: 8001
