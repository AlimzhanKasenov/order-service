controller:
  ingressClass: nginx

  ingressClassResource:
    name: nginx
    enabled: true
    default: false

  hostPort:
    enabled: true

  service:
    type: NodePort

  admissionWebhooks:
    enabled: false
